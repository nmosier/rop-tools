BREAK :=
      MOV rdx,&0x484ba
      ret &0x484ba

#

START:
	INIT
	MOV BYTES
	PUSH
	LIBCALL3 <printf>
	MOV MSG
	PUT 0
	LIBCALL3 <printf>
	JMP $

BYTES:
	db "\ndeadbeef & baddecaf\n\0"

MSG:
	db "yum yum yum\n\0"
